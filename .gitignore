# ----------------------------------------------------------------
# BÍ MẬT & CẤU HÌNH (Rất quan trọng!)
# ----------------------------------------------------------------
# Không bao giờ đưa các file chứa mật khẩu, API keys lên Git.
.env
.env.local
.env.*.local
*.private

# ----------------------------------------------------------------
# THƯ VIỆN (Quan trọng nhất)
# ----------------------------------------------------------------
# Thư mục này chứa hàng ngàn file và ai cũng có thể tự cài đặt
# bằng `npm install` hoặc `yarn install`.
node_modules/

# ----------------------------------------------------------------
# FILE LOG
# ----------------------------------------------------------------
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ----------------------------------------------------------------
# KẾT QUẢ BUILD (Thư mục đầu ra)
# ----------------------------------------------------------------
# Các file này được tạo tự động khi bạn build dự án.
dist/
build/
.output/
.vite/
*.cache

# ----------------------------------------------------------------
# FILE RÁC CỦA HỆ ĐIỀU HÀNH
# ----------------------------------------------------------------
.DS_Store
Thumbs.db

# ----------------------------------------------------------------
# FILE CỦA TRÌNH BIÊN TẬP CODE (IDE)
# ----------------------------------------------------------------
# Các cài đặt cá nhân của VS Code, không nên chia sẻ.
.vscode/
.idea/